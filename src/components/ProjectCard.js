import React from "react";

const ProjectCard = ({
  title,
  projectImg,
  shortDesc,
  gitHubUrl,
  projectUrl,
  projectType,
}) => {
  return <div>ProjectCard</div>;
};

export default ProjectCard;
